<section>
  <%= render 'users/fans' %>
  <% if @users.any? %>
  	<div class="user_avatars">
    	<% @users.each do |user| %>
      		<%= link_to image_tag(user.profile_image), user %>
    	<% end %>
  	</div>
  <% end %>
</section> 